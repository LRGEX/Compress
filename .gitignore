# Implementation docs (internal spec — not committed)
doc/

# Local agent working context (not shared via git)
/AGENTS.md
/agent.md

# Rust build artifacts
/target

# Built binaries / installers (never commit these — they're build output)
*.exe
LRGEX-Compress-*-setup.exe
lrgex-compress-*-setup.exe

# Generated deploy manifest (regenerated by deploy.ps1 each release)
latest.json

# Deploy / release tooling (local, not app source)
deploy.ps1
deploy.bat
register-debug.ps1
unregister-debug.ps1
vendor/unrar/data/

# pi agent working state (local only)
.pi/
latest.json.sig
wiki/
