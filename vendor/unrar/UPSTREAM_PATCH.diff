--- /c/Users/lrg4you/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/unrar-0.5.8/src/open_archive.rs	2006-07-24 04:21:28.000000000 +0300
+++ vendor/unrar/src/open_archive.rs	2026-08-05 14:19:46.351871700 +0300
@@ -3,6 +3,18 @@
 use std::fmt;
 use std::os::raw::{c_int, c_uint};
 use std::path::{Path, PathBuf};
+use std::sync::atomic::{AtomicU64, Ordering};
+
+/// LRGEX: bytes reported by UCM_PROCESSDATA across the current operation.
+pub static PROCESSED_BYTES: AtomicU64 = AtomicU64::new(0);
+
+pub fn processed_bytes() -> u64 {
+    PROCESSED_BYTES.load(Ordering::Relaxed)
+}
+
+pub fn reset_processed() {
+    PROCESSED_BYTES.store(0, Ordering::Relaxed);
+}
 use std::ptr::NonNull;
 
 bitflags::bitflags! {
@@ -526,6 +538,8 @@
                 }
             }
             native::UCM_PROCESSDATA => {
+                let n = if p2 > 0 { p2 as u64 } else { 0 };
+                let _total = PROCESSED_BYTES.fetch_add(n, Ordering::Relaxed) + n;
                 let raw_slice = std::ptr::slice_from_raw_parts(p1 as *const u8, p2 as _);
                 M::process_data(&mut user_data.0, unsafe { &*raw_slice as &_ });
                 0
--- /c/Users/lrg4you/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/unrar-0.5.8/src/lib.rs	2006-07-24 04:21:28.000000000 +0300
+++ vendor/unrar/src/lib.rs	2026-08-05 14:17:46.762998200 +0300
@@ -12,3 +12,5 @@
     CursorBeforeFile, CursorBeforeHeader, FileHeader, List, ListSplit, OpenArchive, Process,
     VolumeInfo,
 };
+
+pub use open_archive::{processed_bytes, reset_processed};
